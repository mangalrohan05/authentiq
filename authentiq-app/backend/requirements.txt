fastapi
# [standard] pulls in websockets/wsproto so the /ws endpoint works — without it
# uvicorn logs "No supported WebSocket library" and realtime features degrade (audit 3.4).
uvicorn[standard]
# Production WSGI/ASGI server — multiple uvicorn workers behind one master (audit 3.2).
gunicorn
# Observability (audit 3.3): Prometheus metrics endpoint + optional Sentry error tracking.
# Both degrade gracefully if absent, so they are safe to omit in constrained envs.
prometheus-client
sentry-sdk[fastapi]
# Required by pydantic EmailStr (used in auth/vendor schemas); without it the
# app raises ImportError at startup ("email-validator is not installed").
email-validator
pymongo
python-dotenv
passlib
# bcrypt 3.2.2 is pinned because passlib 1.7.4 depends on internal bcrypt shims 
# that were removed in bcrypt 4.x, causing AttributeError: module 'bcrypt' has no attribute '__about__'
bcrypt==3.2.2
python-jose[cryptography]
python-multipart
httpx
slowapi
mongomock
pandas
openpyxl
torch>=2.0.0
torchvision>=0.15.0
open-clip-torch>=2.20.0
ultralytics>=8.0.0
pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
imagehash>=4.3.1
scipy>=1.11.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
qrcode[pil]>=8.0.0
# Optional OCR for label/serial verification (install one):
# easyocr
# pytesseract
# Optional heavy OCR engine (enable via AUTHENTIQ_PADDLEOCR_ENABLED=true):
# paddlepaddle
# paddleocr
celery
redis
nest_asyncio
# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-xdist>=3.5.0
httpx>=0.25.0
# Security testing
bandit>=1.7.5
safety>=2.3.5
# Performance testing
locust>=2.17.0
# Code quality
pylint>=3.0.0
black>=23.12.0
mypy>=1.7.0
